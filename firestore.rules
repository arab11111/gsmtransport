rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow reads only for authenticated users; writes are denied.
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
